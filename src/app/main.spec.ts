
// import * as exporting from './main';
// const exporting = require('./main');
describe('subtract method', () => {

    it('subtracts 2 numbers', () => {

        // expect(exporting.getConfig()).toBe(2);
    });

    it('subtracts 2 numbers', () => {
        expect(21).toBe(21);
    });
    it('subtracts 2 number', () => {
        expect(21).toBe(21);
    });
});

